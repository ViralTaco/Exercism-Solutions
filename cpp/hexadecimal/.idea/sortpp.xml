<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="Settings">
    <version>2.0.0</version>
    <sortGroups>
      <sortGroups>
        <name>Static Assertions</name>
        <evaluationOrder>1</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?static_assert \((.*)\)$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
      <sortGroups>
        <name>Friend Declarations</name>
        <evaluationOrder>1</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?friend (.*)$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
      <sortGroups>
        <name>Using Declarations</name>
        <evaluationOrder>1</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?using (.*)$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
      <sortGroups>
        <name>Static Declarations</name>
        <evaluationOrder>1</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?static (.*)$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
      <sortGroups>
        <name>Variables</name>
        <evaluationOrder>2</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?(.*)_(\s*?=.*)?$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
      <sortGroups>
        <name>Functions</name>
        <evaluationOrder>4</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?(.*)$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
      <sortGroups>
        <name>Operators</name>
        <evaluationOrder>3</evaluationOrder>
        <regex>^(public:|protected:|private:)*?\s*?(/\*\*.*?\*/)*?\s*?(template\s*?&lt;.*?&gt;)*?\s*?(.*)operator(.*)$</regex>
        <sortRules>
          <sortRules>
            <matchGroup>4</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
          <sortRules>
            <matchGroup>5</matchGroup>
            <sortOrder>Ascending</sortOrder>
          </sortRules>
        </sortRules>
      </sortGroups>
    </sortGroups>
  </component>
</project>
